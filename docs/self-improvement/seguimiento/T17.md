# Seguimiento T17 — Admisiones y cancelación

## 2026-09-22 · intento 1

**Evidencia:** `target/self-improvement/tasks/T17/resultado.md`.

### Trabajo hecho

- Lógica portable en `admission.rs`; tests `admission`.
- Sondeo guest en `serve_poll.rs` conectado al observador de inferencia.
- 429 busy sin cola; `/health` vía conexiones auxiliares durante generación.
- Cancelación cooperativa (cliente cierra o falla write SSE).

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p soso-llm-api --features std --test admission` | OK |
| `cargo test -p soso-llm-api --features std --test host_service` | OK |
| `cargo build --release -p soso-llm` | OK |

### Pendiente

- Campaña T19 en QEMU (health bajo prefill, segunda gen 429, cierre activo).

### Siguiente paso

- **T18** o **T19** (no abrir el resto del hito en la misma sesión salvo encargo).
