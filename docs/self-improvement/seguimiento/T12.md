# Seguimiento T12 — Respuestas JSON y SSE

## 2026-09-20 · intento 1

**Base:** `e80acd2aa`. **Evidencia:** `target/self-improvement/tasks/T12/resultado.md`.

### Trabajo hecho

- `response.rs`: `CompletionPayload`, `CompletionUsage` desde `GenerationReport`, `finish_reason` (stop/length/tool_calls), JSON `chat.completion`.
- `sse.rs`: stream con deltas de texto o tool call única, `include_usage`, error post-200, exactamente un `[DONE]`, helpers de parse/reconstrucción.

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p soso-llm-api --features std --test response` | 9 OK |
| `cargo test -p soso-llm-api --features std` | OK |
| `cargo check -p soso-llm-api --no-default-features` | OK |

### Limitaciones

- Validación nativa pendiente; el servidor que une runtime + red es T13.

### Siguiente paso

- **T13**.
