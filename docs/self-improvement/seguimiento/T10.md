# Seguimiento T10 — API JSON Chat Completions

## 2026-09-19 · intento 1

**Base:** `9bfcdf5d1`. **Evidencia:** `target/self-improvement/tasks/T10/resultado.md`.

### Trabajo hecho

- Nueva crate [crates/soso-llm-api/](../../../crates/soso-llm-api/): `no_std + alloc`, feature `std`.
- `request.rs`: tipos wire con `deny_unknown_fields`, parseo, `wire_to_chat_input`,
  `validate_wire_policy`, `prepare_chat_completion`, `ApiError` + helpers HTTP.
- `tests/request.rs`: 13 casos (límites, alias, modalidad, Q04, contexto, modelo).

### Decisiones que conviene recordar

- Partes `text` en un mismo mensaje se concatenan con `\n`.
- `max_tokens` / `max_completion_tokens`: error si ambos distintos; default de salida =
  `profile.max_output_tokens` si omitidos; acotado al techo del perfil.
- `temperature` / `top_p` default **1.0** si ausentes (OpenAI); deben ser finitos.
- `model` debe coincidir exactamente con `ModelProfile.id` (`soso-coder`) → 404 si no.
- Presupuesto: `render_messages` + `profile.cabe`; no bytes como proxy de tokens.
- Campos desconocidos en la raíz rechazados; ampliaciones futuras vía T21 + fixture.

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p soso-llm-api --features std --test request` | 13 OK |
| `cargo check -p soso-llm-api --no-default-features` | OK |

### Limitaciones

- Sin HTTP (`http.rs` → T11) ni respuestas/SSE (T12).
- Validación nativa pendiente.

### Siguiente paso

**[T11](../T11-http.md)** — parser HTTP incremental sobre esta crate.
