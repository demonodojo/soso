# Seguimiento T06 — Render Qwen2 con historial completo

## 2026-09-19 · intento 1

**Base:** `07e6ef747`. **Evidencia:** `target/self-improvement/tasks/T06/resultado.md`.

### Trabajo hecho

- `crates/soso-llm-core/src/conversation/render.rs`: plantilla ChatML alineada con
  la `chat_template` oficial de Qwen2.5 (system por defecto, bloque `<tools>`,
  turnos user/assistant/tool, prefijo `assistant` para generar).
- `render_messages` valida entrada, exige vocabulario, ramifica por
  `ModelProfile.family` (`qwen2` implementado).
- `tests/conversation_render.rs`: los cinco fixtures T03 — texto e ids idénticos
  con `target/qwen2.5-coder-3b-model/tokenizer.som`.
- Variantes `ChatError`: `FamiliaNoImplementada`, `TokenizacionInvalida`.
- `chat::render` sin cambios (pregunta única / ask).

### Decisiones que conviene recordar

- Los fixtures HF no traen `tool_call_id` en el turno `tool`; el renderer no lo
  necesita, pero `validate_input` sí — los tests enlazan el resultado con la
  llamada anterior al convertir el fixture.
- JSON compacto vía `serde_json::to_string` + quitar espacios (`tojson` de Jinja),
  compatible con `no_std + alloc`.
- Sin BOS extra: Qwen usa `add_bos_token: false`; la secuencia empieza en
  `<|im_start|>`.

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p soso-llm-core --features std --test conversation_render` | 2 OK (5 fixtures + familia desconocida) |
| `cargo check -p soso-llm-core --no-default-features` | OK |
| Regresión `conversation_validation` / `conversation_types` / `chat::tests` | OK |

### Limitaciones

- Solo familia `qwen2`. Contexto largo (Q10) no se rechaza aquí — T10/T16.
- Validación nativa pendiente (misma lógica en guest, runner T49).

### Siguiente paso

**[T07](../T07-parse-herramientas.md)** — extraer `<tool_call>` de la salida del modelo.
