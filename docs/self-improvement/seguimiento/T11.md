# Seguimiento T11 — HTTP fragmentado

## 2026-09-20 · intento 1

**Base:** `e80acd2aa`. **Evidencia:** `target/self-improvement/tasks/T11/resultado.md`.

### Trabajo hecho

- `crates/soso-llm-api/src/http.rs`: `HttpReader` incremental (`NeedMore` / `Complete` / `HttpError`), límites C3 (`MAX_HEADER_BYTES`, `MAX_BODY_BYTES`), rechazo `Transfer-Encoding: chunked`, una petición por conexión, `signal_eof` para cuerpo incompleto.
- Serialización: `format_response` (CL exacto + `Connection: close`), `format_sse_response_headers` (sin CL).
- `tests/http.rs`: 21 casos (fragmentación, binario, límites ±1, CL ambiguo, EOF, trailing, SSE/JSON).

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p soso-llm-api --features std --test http` | 21 OK |
| `cargo test -p soso-llm-api --features std` | OK (regresión request) |
| `cargo check -p soso-llm-api --no-default-features` | OK |

### Limitaciones

- Validación nativa pendiente; Forja no tocado.

### Siguiente paso

- **T12** — `response.rs` / `sse.rs`.
