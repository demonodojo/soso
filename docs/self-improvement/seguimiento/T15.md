# Seguimiento T15 — Sesión residente

## 2026-09-22 · intento 1

**Base:** `bb08640366888c36b50fea40133d4c95f237e5d0`. **Evidencia:** `target/self-improvement/tasks/T15/resultado.md`.

### Trabajo hecho

- Extraído propietario de sesión a `session.rs` (carga, pool, GPU, catálogo).
- `PropietarioSesion` en askd; `PoliticaModelo::AskHistorica` vs `ApiEstricta`.
- `reset_peticion()` antes de generar en askd (KV por petición, pesos residentes).

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo build --release -p soso-llm` (cwd `user/`) | OK |
| `cargo xtask test -- --guest llm-dense --only ask` | OK (residente + mmap huge) |

### Siguiente paso

- **T16** (no abrir en la misma sesión salvo encargo explícito).
