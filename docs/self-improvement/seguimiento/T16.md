# Seguimiento T16 — Servicio HTTP guest

## 2026-09-22 · intento 1

**Evidencia:** `target/self-improvement/tasks/T16/resultado.md`.

### Trabajo hecho

- Subcomando C4: `soso-llm serve --model … --port … --token-file …`.
- Bucle secuencial ask + HTTP; sesión única vía `preparar_sesion_api` / `PropietarioSesion`.
- API reutiliza `soso-llm-api` (parse, SSE, errores); inferencia vía `generar_para_api`.
- Fallback `chat::render` para modelos sin familia qwen2 (p. ej. `tiny`, un turno user).

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo build --release -p soso-llm` (cwd `user/`) | OK |
| `cargo test -p soso-llm-api --features std --test host_service` | OK |

### Bloqueos / pendiente

- **T14 go** sigue sin cumplir la condición de entrada formal del catálogo.
- Humo guest loopback: **T18** o campaña manual.
- **T17** para cancelación fina y health bajo carga.

### Siguiente paso

- **T17** o **T18** (no abrir T19 hasta T17+T18 según ficha).
