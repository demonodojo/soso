# Seguimiento T18 — Puertos QEMU LLM API

## 2026-09-22 · intento 1

**Evidencia:** `target/self-improvement/tasks/T18/resultado.md`.

### Trabajo hecho

- Construcción pura del `-netdev user` con forward LLM opcional en loopback.
- Shards de test sin forward por defecto; puertos sugeridos `17222 + 10×índice`.
- Run con `SOSO_LLM_HOST_PORT`; colisiones y puertos <1024 rechazados.

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p xtask llm_ports::` | OK (6) |
| `cargo build -p xtask` | OK |

### Siguiente paso

- **T19** (e2e API en QEMU).
