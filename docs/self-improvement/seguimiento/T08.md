# Seguimiento T08 — Informe de generación

## 2026-09-19 · intento 1

**Base:** `a60e8abff`. **Evidencia:** `target/self-improvement/tasks/T08/resultado.md`.

### Trabajo hecho

- `crates/soso-llm-core/src/generation.rs`: `GenerationReport`, `StopReason`,
  `GenerationOptions`, `GenerationLedger` (contadores compartidos).
- `runtime.rs`: bucles `generate_stream_par` y `generate_stream_planned` usan
  el ledger; variante `*_with_report` y `generate_with_report`; API antigua
  (`generate`, `generate_stream*`, `eos: Option<u32>`) sin cambio de tokens.
- `tests/generation_report.rs`: stop inmediato, límite, contexto, multi-stop,
  paridad legacy/report, drafts greedy.

### Decisiones que conviene recordar

- Stop: se cuenta en `sampled_tokens`, no se emite ni se decodifica (igual que
  antes con `eos`, pero con varios ids).
- `ContextLimit` solo cuando no hay planner y `pos >= max_seq`.
- `StopReason::Cancelled` reservado (T09 / observer).
- El caso «stop tras un token emitido» se prueba en unit test del ledger; el
  tiny greedy suele repetir el mismo id y no sirve de fixture E2E para eso.

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p soso-llm-core --features std --test generation_report` | 6 OK |
| `generation::tests::stop_tras_un_token_emitido` | OK |
| Regresión `--test generate` | 10 OK |
| `cargo check -p soso-llm-core --no-default-features` | OK |

### Limitaciones

- Validación nativa pendiente.

### Siguiente paso

**[T09](../T09-cancelacion-runtime.md)** (cancelación) — cerrada 2026-09-19; ver `seguimiento/T09.md`.
