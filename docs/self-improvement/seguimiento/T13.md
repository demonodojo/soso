# Seguimiento T13 — Servidor host

## 2026-09-22 · intento 1

**Base:** `bb08640366888c36b50fea40133d4c95f237e5d0`. **Evidencia:** `target/self-improvement/tasks/T13/resultado.md`.

### Trabajo hecho

- `crates/soso-llm-api/src/service.rs` (`std`): `HostService`, trait `ChatBackend`, `CpuBackend`, `OwnedMapper`, auth Bearer, `/health`, `/v1/models`, `/v1/chat/completions`, busy 429, cancelación vía `CancelBridge`.
- `examples/serve_host.rs`: CLI `--model-dir`, `--profile`, `--port`; token `SOSO_LLM_API_KEY`.
- `tests/host_service.rs`: loopback + backend falso (5 casos).

### Pruebas

| Comprobación | Resultado |
|---|---|
| `cargo test -p soso-llm-api --features std --test host_service` | 5 OK |
| `cargo check -p soso-llm-api --no-default-features` | OK |
| `serve_host` + 2 POST con pesos Qwen | 200 (ver `curl.log`) |

### Limitaciones

- Validación nativa: not_applicable; consumidor T16.
- Tests de integración requieren `target/qwen2.5-coder-3b-model/tokenizer.som` para render qwen2 en `prepare_chat_completion`.

### Siguiente paso

- **T15** (no abrir T14 en la misma sesión).
