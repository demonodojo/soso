# Configuración de `ask`. Cámbiala con: ask-modelo <nombre>
#
# modelo: el de /models que se usa por defecto (ask-modelo lo lista).
#         Si no existe, `ask` coge el primero que encuentre.
# max:    tokens como mucho por respuesta.
modelo=tiny
max=128
temp=0.7
top_p=0.9
