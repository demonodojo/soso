[package]
name = "soso-update"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-only"

[[bin]]
name = "soso-update"
path = "src/main.rs"

[dependencies]
soso-abi = { workspace = true }
libsoso = { workspace = true }
soso-http = { path = "../../crates/soso-http" }
soso-update-core = { path = "../../crates/soso-update-core" }
# Sólo para leer la GPT del disco que se va a poner al día desde el live.
gptdisk = { path = "../../crates/gptdisk" }
