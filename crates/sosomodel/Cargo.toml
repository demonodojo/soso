[package]
name = "sosomodel"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-only"

[features]
default = []
std = []

[dependencies]
crc = "3"
zerocopy = { version = "0.8", features = ["derive"] }
