[package]
name = "soso-llm-core"
version = "0.1.0"
edition = "2024"

[features]
default = []
std = []

[dev-dependencies]
# std feature activada en tests host

[dependencies]
sosomodel = { path = "../sosomodel" }
libm = "0.2"

[lib]
doctest = false
