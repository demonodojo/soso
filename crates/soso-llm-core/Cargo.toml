[package]
name = "soso-llm-core"
version = "0.1.0"
edition = "2024"
license = "GPL-2.0-only"

[features]
default = []
std = []

[dev-dependencies]
soso-audio = { path = "../soso-audio", features = ["std"] }

[dependencies]
sosomodel = { path = "../sosomodel" }
libm = "0.2"

[lib]
doctest = false
