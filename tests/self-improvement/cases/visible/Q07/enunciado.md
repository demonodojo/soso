# Q07 — Argumentos de la llamada partidos en varios deltas

La llamada a herramienta llega en streaming y sus argumentos vienen troceados
entre varios deltas. Solo al concatenarlos todos hay JSON completo: una llamada
ejecutable exige los argumentos enteros y válidos.
