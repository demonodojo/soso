# Q02 — System + user: la instrucción de sistema manda

El mensaje de sistema fija el formato de la respuesta. La respuesta debe
respetarlo: texto que contiene `LISTO`, sin llamadas y con `finish_reason: stop`.
