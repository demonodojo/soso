# Q04 — Llamada a herramienta y su resultado

El historial ya contiene la llamada del asistente y el mensaje `tool` con su
resultado. La respuesta final debe usar ese resultado y **no** volver a llamar
a la herramienta.
