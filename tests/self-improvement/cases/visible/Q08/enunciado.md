# Q08 — tool_choice con un nombre que no existe

`tool_choice` fija una herramienta que no está en `tools`. Eso es una petición
inválida: 400 con objeto `error`, y **sin** texto de asistente inventado.
