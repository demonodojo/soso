# Seguimiento — page fault de red

Una fila por avistamiento o por experimento. No borrar las anteriores.

| Fecha | Qué se vio | ELF | Conclusión |
|-------|------------|-----|------------|
| 2026-09-21 | `soso-update comprobar`. Dos TLS a `140.82.121.4:443`, DNS de `release-assets`, pánico antes de imprimir el `conectado` de `185.199.111.133`. `page fault at 0x153451e79a8` `rip=0x1000043a5ed` `cs=0x8` `err=0x0`. Centinelas: 57 vivos, 0 desbordados. `rsp` en KSTACK de la BSP. | El del pendrive de esa sesión (`interrupts.rs` pasó de ~412 a 442 al añadir el volcado). | `0x43a5ed` = `talc::llist::IterMut::next`. `rax` ya traía el puntero. `+0x40` = `Talc::malloc`. No es `lx_kmalloc`, ni DF, ni las copias de RX/TX/FB revisadas. El store no se encontró. |
| 2026-09-21 (reflasheado) | El mismo pánico con el `cld` ya en `irq::dispatch`. Llegó a resolver `release-assets` → `185.199.111.133` y petó al abrir esa conexión. Misma firma, 57 centinelas. | Kernel con `cld` en el dispatch. | El `cld` no era la causa. |
| 2026-09-24 | Foto: `conectado (fd 4)`, DNS `release-assets.githubusercontent.com` → `185.199.111.133`, `conectando` a `:443`, `conectado (fd 4)`, luego el volcado. `interrupts.rs:442`. Centinelas: 67 vivos, 0 desbordados. El puntero malo repetido en el volcado en `+0x10` `+0x18` `+0x20` `+0x28`. OCR del CR2: `0x153d51e79a0` / `0x153d51a79a0`. | No confirmado en el pendrive. En disco, `target/kernel/x86_64-soso/debug/kernel` es del 24 sep 09:18; ahí `0x43a5ed` cae dentro de `IndexMut<[u8; 12]>` y esos cuatro offsets son spills de rdi/rdx/rsi. | Misma firma. Falta saber si el USB llevaba el ELF del 09:18 o el del 21 sep. Sin eso no se mezclan las dos traducciones del RIP. Store sigue sin localizar. |
| 2026-09-24 | Sonda `mm::heap::punto` en DNS, `net::poll` y `tcp-buf`. El pánico imprime `heap: último punto intacto`. | Este árbol, aún sin flashear. | El store no se ha visto. La próxima foto tiene que traer esa línea. |
| 2026-09-24 11:31 | Foto. `listas OK tras resolve_a` y `tras liberar servidores DNS` (ptr `0x444445184f28`, len 5, cap 8). Luego `conectando a 140.82.121.4:443`. `heap: último punto intacto «poll-entra»`. `interrupts.rs:446`. CR2 `0x1d0000000003d`, `rip=0x10000045e03d`, `rsp` en KSTACK, `err=0` (la instrucción es un store: el bit de escritura puede haberse leído mal), 57 centinelas, 0 desbordados. | `/media/…/KERNEL/kernel-x86_64` de esa ESP (ELF con debug, 67 MiB, contiene `poll-entra`). `SOSOHASH.TXT` sigue diciendo 0.3.1 del 19 sep; no describe este binario. | `0x45e03d` cae en `mov %rcx, 0x10(%rax)` de `bitflags::IterNames<virtio_drivers::device::net::Features>::next` (`iter.rs:139`). `rax` sale de `0x50(%rsp)` y `rax+0x10` es el CR2: el destino del `Some((nombre, flags))` no está mapeado. Encaja con `try_attach` → `virtio_net::init` justo después de `poll-entra`. El `Vec` de DNS queda descartado. |
| 2026-09-24 12:15 | Foto. DNS con listas OK. `conectado (fd 4)` hacia `185.199.111.133`. `heap: último punto intacto «poll-nicho»`. `interrupts.rs:448`. CR2 `0x3500000000`, `rip=0x1000043a00d`, `err=0`, 57 centinelas, 0 desbordados. | `kernel-x86_64` de esa ESP (tiene `poll-nicho` y `poll-rx`). | `poll-nicho` volvió: `try_attach` ya no es el sitio. El `rip` está en `smoltcp::wire::ip::checksum::data` (`ip.rs:797`, `data.first()`), dentro del primer `iface.poll`. El byte leído es `0x3500000000`, fuera de la pila y del heap. |
| 2026-09-24 12:56 | Foto. `conectado (fd 4)` a `140.82.121.4`. `poll-nicho`. `red: última trama rx ptr=0x10000a97648 len=64`. `interrupts.rs:453`. Centinelas 57/0. CR2 leído `0x16700000000`, `rip` leído `0x1000043ae41d`, `err=0`. | `kernel-x86_64` de esa ESP (67 MiB, fecha 1980 del FAT). `.text` acaba en `0x5083ab`. | La trama RX es la pila, 64 bytes: el puntero malo no entra con el paquete. Sigue siendo `rx`, así que el fallo es dentro del `consume` de esa trama, antes de un TX. El offset `0x43ae41d` cae fuera de este ELF; no se simboliza. La lectura más cercana en rango es `cmpq $-1, (%rdi)` en `ManagedSlice<PacketMetadata<UdpMetadata>>::deref_mut` (`slice.rs:94`, `0x43ae45`), pero los dígitos no coinciden y no se da por hecho. |
| 2026-09-24 13:13 | Foto. `conectado (fd 4)` a `140.82.121.3`. `poll-nicho`. `red: última trama rx ptr=0x10000a9761c len=86`. `pf: ins` leído `48 8b 5d/45 48 48 8b 11 48` (no está en el ELF). `interrupts.rs:454`. CR2 `0x21600000000`. `rip` leído `0x1000043e9bd`. `err=0`. Centinelas 57/0. | `kernel-x86_64` de esa ESP. | `0x43e9bd` cae a mitad de `mov %rsp, %rdi` dentro de `<*mut u8 as Pointer>::fmt` instanciado en talc (`fmt/mod.rs:3051`). Esa instrucción no lee. La lectura anterior es `mov (%rdi), %rax` en `0x43e9ae`: carga el puntero que se está imprimiendo. talc formatea punteros en el `Debug` de `Span` y en el `debug_assert` de `talc.rs:110`. La trama de 86 bytes sigue en la pila. |
| 2026-09-24 13:49 | Foto. `soso-update` descarga 12,6 MiB de 135,1 MiB y entonces `heap: tabla de centinelas llena (4096)` en `heap.rs:110`. Centinelas lxdde no salen. No hay page fault. | Este árbol, con centinela en cada `alloc` de Rust. | La tabla cuenta reservas vivas. `punto` hacía `mem::forget` en cada `poll` y la llenó. La descarga pasó el sitio del page fault anterior. |
| 2026-09-24 14:22 | Foto. `conectado (fd 4)` a `185.199.108.133`. `poll-nicho`. `red: última trama tx ptr=0x10000a85d6 len=54`. `interrupts.rs:464`. CR2 `0x3250000000`. `rip=0x1000043f13d`. `err=0`. Centinelas lxdde 57/0. No sale `BLOQUE DESBORDADO`. | `kernel-x86_64` de esa ESP. Bytes en el `rip`: `48 8b 10 48 89 11`. | `0x43f13d` = `mov (%rax), %rdx` en `IterMut::next` (`llist.rs:98`), el `next` del nodo libre. `rax` es `0x3250000000`. Los centinelas de Rust y de `lx_kmalloc` estaban enteros: no es un desbordamiento lineal de un bloque vivo. El enlace `next` de la lista libre es el que apunta fuera. |
| 2026-09-24 17:11 | Foto. `poll-nicho`. `red: última trama tx` len=54, ptr en la KSTACK. `pf: ins 48 8b 10 48 89 11`. Centinelas lxdde 57/0. `heap: buscando CR2` en 512 MiB, **1 coincidencia**, dentro del heap (`0x4444…`). `interrupts.rs:465`. CR2 leído `0x5500000000`. `rip=0x1000043f9ad`. `err=0`. Debajo: `conexión rechazada (errno 11)` y `E/S falló` en `tcp_connect`. | `target/kernel/x86_64-soso/debug/kernel` de las 17:05 (36 MiB). El `rip` cae en una instrucción real de ese ELF. | `0x43f9ad` = `mov (%rax), %rdx` y el byte siguiente `48 89 11` = `mov %rdx, (%rcx)`, `IterMut::next` (`llist.rs:98`). Misma carga que a las 14:22. El puntero malo está almacenado **una vez** en el heap; no sólo en un registro. La dirección exacta del qword no se fija: la foto mueve dígitos de `0x4444…`. |
| 2026-09-24 19:10 | Código (sin foto nueva). `heap_talc_audit::vigilar_huecos`, histórico alloc/free con `ra`, puntos `rx-consumido`/`tx-consumido`, `lx_iwlwifi_dma_ranges`, cuarentena en `lx_dma_free_coherent`, `informar_dma_en_pf` en el manejador de #PF. | Árbol actual; flashear live USB con perfil iwlwifi. | **Pendiente:** reproducir en placa y leer `HUECO ROTO` o coincidencia DMA. La corrección del store va en la fila siguiente cuando haya `ra` o rango DMA. |
| 2026-09-24 19:26 | Foto. Tras flashear, `soso-update comprobar` (origen GitHub) → **#GP** `general protection fault` `rip=0x10001f3caf` `rsp=0x1000075ca80`, panic en `exception_panic_shim` (`interrupts.rs:417`). | ELF debug en disco: `0x1f3caf` = lectura de `bins.add(bin)` en `heap_talc_audit::bin_head` (layout talc). | La auditoría leía `bins` sin comprobar que cae en el heap mapeado → #GP. Corregido: espejo `TalcLayout` + `ClaimOnOom`, assert de tamaño, omitir si `bins` fuera de rango, `read_volatile` en la tabla de bins. |
| 2026-09-24 19:33 | Foto + SOSOLOG del USB. `poll-nicho`. `red: última trama tx len=207`. `pf: ins 48 8b 10 48 89 11`. `heap: coincidencia 1 en 0x44444444…` (primeros 64 KiB del heap). `dma: valor 0xe74c81da30 no es VA del heap ni mapeado como virt`. `interrupts.rs:466`. CR2 `0xe74c81da30`. `rip` leído `0x10000442d0d` (no encaja con los bytes: `0x442d0d` es `handle_oom`; los bytes son `IterMut::next`). SOSOLOG: 197 k líneas de `vigilar_huecos(…) omitido (bins=0x2048001080000823 …)`: la auditoría nunca recorrió nada. | `kernel-x86_64` de esa ESP. | `Talc` es `repr(Rust)`: el espejo `TalcLayout` leía la máscara de disponibilidad como `bins`. **Lectura nueva:** los CR2 de todas las fotos (`0x55`, `0x32`, `0x35`, `0xe7`, `0x153`, `0x167`, `0x216` en los bytes +4/+5, y a veces la mitad baja de un puntero del heap: `0xe7_4c81da30`, `0x153_451e79a8`) son un **`u16` escrito en los bytes +4/+5 de un qword** cuya mitad baja era una cabecera de bin. La coincidencia en `0x44444444…` cae donde `claim` deja la tabla `bins` (`HEAP_START+8`). Hipótesis: alguien escribe un `u16` (¿longitud de trama?) en `HEAP_START+0xc` = `bins[0]+4`. |
| 2026-09-24 20:05 | Código. `arch::hwbp` (DR0/DR1 = `bins[0]`/`bins[1]`, escritura, 8 bytes, por CPU; APs lo arman en `ap_entry`). Handler `#DB` en `interrupts.rs`: si talc tiene su candado y el valor cae en el heap, vuelve; si no, `heap: ESCRITURA EN bins (DRn) addr valor rip rsp cpu candado`, rastro de pila, últimos alloc/free, `informar_dma_en_pf` y panic con el offset para `addr2line`. `heap_talc_audit::bins_ptr` busca la palabra que vale `TALC_BINS` en vez de fiarse del orden de campos. `localizar_en_pf` dice `la coincidencia es bins[N] de talc`. `omitido` sólo se imprime una vez. QEMU/KVM arranca hasta `sosh` con red sin `#DB` falso. | Árbol actual; falta flashear. | **Pendiente.** Si la foto trae `ESCRITURA EN bins`, el `rip` es el store y se corrige ahí. Si trae `HUECO ROTO` en `bin=0` sin `#DB`, el que escribe es DMA (firmware iwlwifi o GSP) hacia el frame físico de `HEAP_START`. |
| 2026-09-24 ~20:50 | Tras flashear kernel 67 MiB (`bootmark.txt` 20:47). Dos `soso-update comprobar` seguidos **sin** #PF. SOSOLOG: DNS + `github.com` + `release-assets` → `conectado (fd 4)`, `ya estás en la última versión`, ~12,8 MiB descargados (manifiesto/kernel), `heap: listas OK` en resolve/liberación DNS. Sin `EXCEPTION`, `HUECO ROTO`, `ESCRITURA EN bins` ni líneas `vigilar_huecos` (auditoría OK = silenciosa). Sondas heap en `0x44444c81da20` (segunda pasada). | ESP `/media/jmdiez/KERNEL`, SOSOLOG 262 KiB. | No prueba arreglo definitivo: el fallo ya era esporádico (carrera / timing WiFi). Los watchpoints en `bins` siguen armados; si el store vuelve a ocurrir debería salir `#DB` o `HUECO ROTO` antes del #PF clásico. Repetir comprobar en caliente o forzar más tráfico (descarga real, ping largo) para intentar reproducir. |
